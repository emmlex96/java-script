cost product = (price 1200, instock: true)

if (product.instock == false){
    let meassage = "out of stock"
    reurn message
    }else if (product.price > 1000){
        message = "expensive"
    }else if message = "Affordable"
        console.log(message)}
