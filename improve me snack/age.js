function getUserAge(age){

    if(age < 0){
    return "Invalid age"
    
    }
    if (age < 5){
    return "child";
    }else if{
    (age < 18)
    return "Teen";
    }else{
    return "Adult";
    }
    
    
    let result = getUserAge(age);
        condole.log(result);
